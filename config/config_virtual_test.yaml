#config_version=6

# Virtual test helpers (keyboard via gmc.cfg). Loaded only by config.yaml
# (dev/virtual entry), not by future hardware entry configs.

custom_code:
  - code.virtual_test.VirtualTestHelper
